# jsonresume-theme-jake-resume-extra

A standalone fork of the Minimal JSON Resume theme with additional display
controls, including aliases, custom ordering, censorship, display-date text,
URL labels, and string locations.

Run npm test or npm run preview from this directory.
