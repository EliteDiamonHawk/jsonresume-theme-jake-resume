# jsonresume-theme-jake-resume-minimal

The schema-only JSON Resume theme. It exports render(resume) and accepts
standard JSON Resume fields without custom display controls.

Run npm test or npm run preview from this directory.
